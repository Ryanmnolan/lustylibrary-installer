# Lusty Library Installer

A setup wizard for building an offline ebook/audiobook server on a Raspberry Pi.

## Quick install

```bash
curl -sSL https://raw.githubusercontent.com/<YOURUSER>/lustylibrary-installer/main/install.sh | sudo bash
